# SiliconFlow（硅基流动）AI 搭配引擎所需的服务端密钥。
# 仅在服务端 app/api/style/route.ts 读取，绝不下发前端，禁止使用 NEXT_PUBLIC_ 前缀。
# 复制本文件为 .env.local 后填入真实 Key；不配置时 AI 搭配会自动走本地规则兜底。
SILICONFLOW_API_KEY=这里粘贴你的_SiliconFlow_API_Key

# 可选：覆盖默认模型 / Base URL（不填用代码内默认值）
# 默认文字搭配与识图都用 Qwen3-VL-30B-A3B-Instruct（视觉+文本、非推理、JSON 直出、速度快）
# SILICONFLOW_MODEL=Qwen/Qwen3-VL-30B-A3B-Instruct          # 文字搭配引擎
# SILICONFLOW_VISION_MODEL=Qwen/Qwen3-VL-30B-A3B-Instruct   # /add 自动识图（多模态）
# SILICONFLOW_BASE_URL=https://api.siliconflow.cn/v1
# 注意：该模型为 Instruct（非推理），请求体不要带 enable_thinking 参数（会 400）。

# 说明：识图（/add 自动识别类别/颜色/名称）必须连真实模型，无法本地兜底；
# 未配置 Key 时识图自动跳过，用户手动选类别即可，不影响录入。
